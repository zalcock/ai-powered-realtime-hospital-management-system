# URL of the backend API (no trailing slash, no /api suffix)
VITE_API_URL="http://localhost:5000"

# URL of the backend Socket.IO server (usually the same host as the API)
VITE_SOCKET_URL="http://localhost:5000"
